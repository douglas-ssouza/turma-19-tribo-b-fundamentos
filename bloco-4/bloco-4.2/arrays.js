// Variavel ou constante que recebe varios valores/elementos
// 0 - 1 - 2 - 3 - 4
const numeros = [3, 5, 6, 7, 8];
console.log(numeros);

numeros[0] = 4;
numeros[1] = 10;
numeros[2] = 12;
console.log(numeros);

numeros.push(100);
console.log(numeros);

numeros.pop();
console.log(numeros);

const pessoasEstudantes = ['Otavio', 'Rafa', 'Joaõ', 'Gabriel', 'Ale'];
pessoasEstudantes.push('Debora');
console.log(pessoasEstudantes);
