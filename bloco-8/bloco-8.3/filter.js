const { numbers, names, students } = require('./arrays');

// Filtre o array numbers por números positivos (incluindo 0)
// const onlyPositives = numbers.filter((number) => number >= 0);
// console.log(onlyPositives);

// Filtre o array numbers por números negativos
// const onlyNegatives = numbers.filter((number) => number < 0);
// console.log(onlyNegatives);

// Filtre o array numbers por números pares/impares
// const checkEven = (number) => number % 2 === 0;
// const onlyEvens = numbers.filter(checkEven);
// console.log(onlyEvens);

// const checkOdd = (number) => number % 2 !== 0;
// const onlyOdds = numbers.filter(checkOdd);
// console.log(onlyOdds);

// Filtre o array names por nomes que comecem com a letra R/r
// const checkR = (name) => name.toUpperCase().startsWith('R');
// const startWithR = names.filter(checkR);
// console.log(startWithR);

// Filtre o array names por nomes que tenham mais de 5 caracteres
// const maiorQue5 = (name) => name.length > 5;
// const algumNome = names.filter(maiorQue5);
// console.log(algumNome);

// Filtre o array names por nomes que incluam a letra a
// const hasLetterA = names.filter(
//   (name) => name.toLowerCase().includes('as') || name.toLowerCase().includes('á')
// );
// console.log(hasLetterA);

// Filtre o array students por pessoas com idade maior/menor que 30
// const maiorQue30 = students.filter((student) => student.age > 30);
// console.log(maiorQue30);

// students.filter((student) => student.age < 30);
// const soNomes = menorQue30.map((pessoa) => pessoa.name);
// console.log(soNomes);

// Filtre o array students por pessoas com CSS como uma tech favorita
// const filterByCss = students.filter(
//   (student) => student.favTechs.join('').toLowerCase().includes('css')
// );
// console.log(filterByCss);

// Filtre o array students por pessoas com Jest e React como tech favoritas

// Filtre o array students por pessoas com JavaScript ou Jest como techs favoritas
