const tryber = {
  name: 'Rods',
  age: 20,
  address: {
    city: 'Belo Horizonte',
    state: 'Minas Gerais',
    street: 'Avenida Pão de Queijo',
    number: 33,
  },
};

// const a = 'name';
// const b = 'age';

// tryber.name = 'Zezé';
// tryber['name'] = 'Ellen';

// tryber[a] = 'Matheus';
// tryber['b'] = 100,

// const alteraObjeto = (objeto, chave, valor) => {
//   objeto[chave] = valor;
// };
// alteraObjeto(tryber, 'name', 'Tássio');

// console.log(tryber);


// const keys = Object.keys(tryber.address);
// console.log(keys);
// const values = Object.values(tryber.address);
// console.log(values);
// const entries = Object.entries(tryber.address);
// console.log(entries);

